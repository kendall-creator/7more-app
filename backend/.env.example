# IMPORTANT: Server-side secrets only - NEVER commit to git or expose to client
# These are for the backend server only

# Gmail SMTP Configuration (from your existing setup)
BRIDGE_TEAM_EMAIL=bridgeteam@7more.net
BRIDGE_TEAM_EMAIL_PASSWORD=weacvrkmtgunrbek
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false

# API Authentication (this is the secret the backend checks)
EMAIL_API_KEY=bridge-email-v1-7more-secure-2025

# Server Configuration
PORT=3001
NODE_ENV=production
